# Agent Skills

Repo-local skill notes for agents working on Kolabs Sample Management.

Read the relevant note before acting:

- `ctx7.md` for library/framework/API changes.
- `design-system.md` for applying `DESIGN.md` to UI and product polish.
- `frontend-quality.md` for UI and frontend behavior work.
- `release-ci.md` for desktop installer, GitHub Actions, and release work.
- `repo-workflow.md` for branch, commit, merge, verification, and push flow.

These notes complement the root `AGENTS.md`; they do not replace it.
