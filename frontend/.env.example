# Only needed if the API is on a DIFFERENT origin than the web app.
# Leave unset for same-origin (dev uses Vite proxy; prod served by FastAPI).
# VITE_API_BASE=https://api.example.com

# Override the dev proxy target if the backend runs elsewhere:
# VITE_API_PROXY=http://127.0.0.1:8000
